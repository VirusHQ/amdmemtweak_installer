# amdmemtweak_installer

Use this to install the Vega Tweak Tool that helps one change the bios timings for better performance for vega gpus. 

To install the tweak tool on ethos , run : source <(curl -s URL_HERE ) 
